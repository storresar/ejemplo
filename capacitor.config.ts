import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ejemplo',
  appName: 'ejemplo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
